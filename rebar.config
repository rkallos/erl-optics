{erl_opts, [debug_info]}.
{deps, [
  {foil, "0.1.1"}
]}.

{pre_hooks,
  [{"(linux)", compile, "make -C c_src"}]}.

{post_hooks,
  [{"(linux)", clean, "make -C c_src clean"}]}.

{profiles, [
  {test, [
    {erl_opts, [export_all]}
  ]}
]}.
